/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StoryOverviewDto } from '../../models/story-overview-dto';
import { StoryOverviewRequest } from '../../models/story-overview-request';

export interface GetStoryOverview$Params {
      body: StoryOverviewRequest
}

export function getStoryOverview(http: HttpClient, rootUrl: string, params: GetStoryOverview$Params, context?: HttpContext): Observable<StrictHttpResponse<StoryOverviewDto>> {
  const rb = new RequestBuilder(rootUrl, getStoryOverview.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StoryOverviewDto>;
    })
  );
}

getStoryOverview.PATH = '/story/story-overview';
