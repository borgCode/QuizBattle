/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AllStoriesDto } from '../../models/all-stories-dto';

export interface GetAllStories$Params {
  playerId: number;
}

export function getAllStories(http: HttpClient, rootUrl: string, params: GetAllStories$Params, context?: HttpContext): Observable<StrictHttpResponse<AllStoriesDto>> {
  const rb = new RequestBuilder(rootUrl, getAllStories.PATH, 'post');
  if (params) {
    rb.path('playerId', params.playerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AllStoriesDto>;
    })
  );
}

getAllStories.PATH = '/story/all/{playerId}';
