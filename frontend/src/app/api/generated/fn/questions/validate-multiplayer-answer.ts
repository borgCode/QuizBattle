/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnswerValidationResponse } from '../../models/answer-validation-response';
import { MultiplayerAnswerValidationRequest } from '../../models/multiplayer-answer-validation-request';

export interface ValidateMultiplayerAnswer$Params {
      body: MultiplayerAnswerValidationRequest
}

export function validateMultiplayerAnswer(http: HttpClient, rootUrl: string, params: ValidateMultiplayerAnswer$Params, context?: HttpContext): Observable<StrictHttpResponse<AnswerValidationResponse>> {
  const rb = new RequestBuilder(rootUrl, validateMultiplayerAnswer.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AnswerValidationResponse>;
    })
  );
}

validateMultiplayerAnswer.PATH = '/questions/session/validate-answer';
