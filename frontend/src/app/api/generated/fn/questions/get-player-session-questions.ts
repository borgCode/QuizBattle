/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionDto } from '../../models/question-dto';

export interface GetPlayerSessionQuestions$Params {
  playerId: number;
}

export function getPlayerSessionQuestions(http: HttpClient, rootUrl: string, params: GetPlayerSessionQuestions$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QuestionDto>>> {
  const rb = new RequestBuilder(rootUrl, getPlayerSessionQuestions.PATH, 'get');
  if (params) {
    rb.path('playerId', params.playerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QuestionDto>>;
    })
  );
}

getPlayerSessionQuestions.PATH = '/questions/{playerId}/questions';
