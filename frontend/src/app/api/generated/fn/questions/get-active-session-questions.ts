/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionDto } from '../../models/question-dto';

export interface GetActiveSessionQuestions$Params {
  sessionId: number;
  playerId: number;
}

export function getActiveSessionQuestions(http: HttpClient, rootUrl: string, params: GetActiveSessionQuestions$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QuestionDto>>> {
  const rb = new RequestBuilder(rootUrl, getActiveSessionQuestions.PATH, 'get');
  if (params) {
    rb.path('sessionId', params.sessionId, {});
    rb.path('playerId', params.playerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QuestionDto>>;
    })
  );
}

getActiveSessionQuestions.PATH = '/questions/session/{sessionId}/{playerId}';
