/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageNotification } from '../../models/page-notification';

export interface GetArchivedNotifications$Params {
  playerId: number;
  pageable: Pageable;
  searchFilter?: string;
  timeFilter?: 'ALL_TIME' | 'TODAY' | 'THIS_WEEK' | 'THIS_MONTH';
}

export function getArchivedNotifications(http: HttpClient, rootUrl: string, params: GetArchivedNotifications$Params, context?: HttpContext): Observable<StrictHttpResponse<PageNotification>> {
  const rb = new RequestBuilder(rootUrl, getArchivedNotifications.PATH, 'get');
  if (params) {
    rb.path('playerId', params.playerId, {});
    rb.query('pageable', params.pageable, {});
    rb.query('searchFilter', params.searchFilter, {});
    rb.query('timeFilter', params.timeFilter, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageNotification>;
    })
  );
}

getArchivedNotifications.PATH = '/notification/archived/{playerId}';
