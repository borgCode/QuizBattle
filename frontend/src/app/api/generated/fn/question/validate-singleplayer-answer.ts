/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnswerValidationResponse } from '../../models/answer-validation-response';
import { SinglePlayerAnswerValidationRequest } from '../../models/single-player-answer-validation-request';

export interface ValidateSingleplayerAnswer$Params {
      body: SinglePlayerAnswerValidationRequest
}

export function validateSingleplayerAnswer(http: HttpClient, rootUrl: string, params: ValidateSingleplayerAnswer$Params, context?: HttpContext): Observable<StrictHttpResponse<AnswerValidationResponse>> {
  const rb = new RequestBuilder(rootUrl, validateSingleplayerAnswer.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AnswerValidationResponse>;
    })
  );
}

validateSingleplayerAnswer.PATH = '/question/chapter/validate-answer';
