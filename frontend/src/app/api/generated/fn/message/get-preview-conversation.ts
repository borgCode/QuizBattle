/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConversationPreviewDto } from '../../models/conversation-preview-dto';

export interface GetPreviewConversation$Params {
  conversationId: number;
  playerId: number;
}

export function getPreviewConversation(http: HttpClient, rootUrl: string, params: GetPreviewConversation$Params, context?: HttpContext): Observable<StrictHttpResponse<ConversationPreviewDto>> {
  const rb = new RequestBuilder(rootUrl, getPreviewConversation.PATH, 'post');
  if (params) {
    rb.path('conversationId', params.conversationId, {});
    rb.query('playerId', params.playerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ConversationPreviewDto>;
    })
  );
}

getPreviewConversation.PATH = '/messages/conversation/{conversationId}/player/{playerId}';
