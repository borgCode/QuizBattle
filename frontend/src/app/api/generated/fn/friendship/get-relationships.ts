/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RelationshipsDto } from '../../models/relationships-dto';

export interface GetRelationships$Params {
  playerId: number;
}

export function getRelationships(http: HttpClient, rootUrl: string, params: GetRelationships$Params, context?: HttpContext): Observable<StrictHttpResponse<RelationshipsDto>> {
  const rb = new RequestBuilder(rootUrl, getRelationships.PATH, 'get');
  if (params) {
    rb.path('playerId', params.playerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RelationshipsDto>;
    })
  );
}

getRelationships.PATH = '/friendship/relationships/{playerId}';
